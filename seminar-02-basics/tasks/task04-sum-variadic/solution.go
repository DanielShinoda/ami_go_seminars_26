package variadic

// Sum складывает произвольное количество чисел.
// Sum() == 0, Sum(1, 2, 3) == 6.
//
// Сигнатуру менять нельзя: троеточие перед типом — это variadic-параметр.
func Sum(nums ...int) int {
	panic("TODO: реализуйте Sum")
}

// MaxOf возвращает максимум из аргументов.
// Первый аргумент обязателен — так функцию НЕВОЗМОЖНО вызвать без данных,
// и не нужно возвращать признак ошибки. Это частый приём в Go.
func MaxOf(first int, rest ...int) int {
	panic("TODO: реализуйте MaxOf")
}

// Average возвращает среднее арифметическое и ok = false для пустого вызова.
func Average(nums ...int) (float64, bool) {
	panic("TODO: реализуйте Average")
}
