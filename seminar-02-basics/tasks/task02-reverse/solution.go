package reverse

// Reverse разворачивает строку задом наперёд.
//
// Внимание: разворачивать нужно по РУНАМ, а не по байтам.
// Наивный разворот байтов сломает кириллицу и превратит строку в мусор.
//
// Reverse("Привет") == "тевирП"
//
// Подсказка: []rune(s) даёт слайс рун; менять элементы местами можно так:
//
//	r[i], r[j] = r[j], r[i]
//
// Обратно в строку — string(r).
func Reverse(s string) string {
	panic("TODO: реализуйте Reverse")
}

// IsPalindrome сообщает, читается ли строка одинаково в обе стороны.
// Сравнение регистрозависимое, пробелы и знаки препинания НЕ игнорируются.
func IsPalindrome(s string) bool {
	panic("TODO: реализуйте IsPalindrome")
}
