package counter

// New возвращает функцию-счётчик. Каждый её вызов возвращает
// следующее число, начиная с 1:
//
//	next := New()
//	next() // 1
//	next() // 2
//
// Счётчики, полученные разными вызовами New, независимы друг от друга.
//
// Это задача про ЗАМЫКАНИЕ: переменная объявляется в New,
// а живёт внутри возвращённой функции.
func New() func() int {
	panic("TODO: реализуйте New")
}

// Accumulator возвращает функцию, которая прибавляет аргумент к внутренней
// сумме и возвращает новое значение суммы:
//
//	acc := Accumulator(10)
//	acc(5)  // 15
//	acc(-3) // 12
func Accumulator(start int) func(int) int {
	panic("TODO: реализуйте Accumulator")
}
