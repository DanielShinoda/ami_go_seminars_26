package collatz

// Steps возвращает количество шагов последовательности Коллатца,
// нужное чтобы из n прийти к 1.
//
// Правило одного шага:
//
//	n чётное  -> n / 2
//	n нечётное -> 3*n + 1
//
// Steps(1) == 0, Steps(6) == 8 (6→3→10→5→16→8→4→2→1).
// Для n <= 0 вернуть -1: последовательность не определена.
func Steps(n int) int {
	panic("TODO: реализуйте Steps")
}

// MaxValue возвращает наибольшее значение, которого достигает
// последовательность Коллатца, стартовав с n (включая сам n).
// Для n <= 0 вернуть -1.
//
// Например, для n = 3: 3→10→5→16→8→4→2→1, максимум = 16.
func MaxValue(n int) int {
	panic("TODO: реализуйте MaxValue")
}
